using System;
using System.Collections.Generic;
using System.IO;
using System.Linq;
using System.Threading.Tasks;

namespace Kustodya.WebApi.Services
{
    public class ImageService //: IImageService
    {
        //public async Task<MemoryStream> ConvertToTiff(MemoryStream stream)
        //{
        //    using (MemoryStream memStream = new MemoryStream())
        //    {
        //        using (MagickImage image = new MagickImage(new MemoryStream(stream.ToArray()))
        //        {
        //            Format = MagickFormat.Tiff,
        //            Depth = 8
        //        })
        //        {
        //            image.Write(memStream);
        //            return memStream;
        //        }
        //    }
        //}


        //public async Task<MemoryStream> ConvertToPpm(MemoryStream stream)
        //{
        //    using (MemoryStream memStream = new MemoryStream())
        //    {
        //        using (MagickImage image = new MagickImage(new MemoryStream(stream.ToArray()))
        //        {
        //            Format = MagickFormat.Ppm,
        //            Depth = 8
        //        })
        //        {
        //            image.Write(memStream);
        //            return memStream;
        //        }
        //    }
        //}
    }
}
